#include <iostream>
#include <utility>
#include <vector>

int maxProfit(std::vector<std::pair<int, int>> &input)
{
}

int main(int argc, char const *argv[])
{
  std::vector<std::pair<int, int>> goods{
      std::make_pair<int, int>(1, 5),
      std::make_pair<int, int>(2, 15),
      std::make_pair<int, int>(3, 24),
      std::make_pair<int, int>(4, 30),
      std::make_pair<int, int>(5, 35),
  };

  return 0;
}
