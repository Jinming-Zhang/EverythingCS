Database server manage multiple databases

Each database has multiple tables

Each table has entries (same type of entity) of data

Schema -> synonym for Database in MySql

Schema -> kind of namespace for Postgres